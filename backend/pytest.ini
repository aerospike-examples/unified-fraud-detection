[pytest]
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests that require the live docker stack (localhost:4000)
    stress: heavier concurrency / timing tests (still offline; uses mock engine)
